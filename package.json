{
  "name": "sysa",
  "version": "0.0.1",
  "scripts": {
    "test": "jest --forceExit --coverage --verbose",
    "test-dev": "jest --forceExit --watch"
  },
  "dependencies": {
    "typescript": "^2.6.2"
  },
  "devDependencies": {
    "jest": "^22.1.4",
    "ts-jest": "^22.0.1"
  }
}
